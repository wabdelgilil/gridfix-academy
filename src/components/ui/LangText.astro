---
interface Props {
  lang: 'ar' | 'en';
  class?: string;
}
const { lang, class: className } = Astro.props;
const dir = lang === 'ar' ? 'rtl' : 'ltr';
---

<span data-lang={lang} lang={lang} dir={dir} class:list={[className]}>
  <slot />
</span>